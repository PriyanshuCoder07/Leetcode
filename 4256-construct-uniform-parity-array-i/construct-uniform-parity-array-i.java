class Solution {
    public boolean uniformArray(int[] nums1) {
        // int n=nums1.length;
        // int ec=0,oc=0;
        // for(int i=0; i<nums1.length; i++){
        //     if(nums1[i]%2==0) ec++;
        //     else oc++;
        // }
        // if(ec==n || oc==n) return true;
        // else if(ec==n/2 && oc==n/2) return true;
        return true;
    }
}